= form_for @tree do |f|
  - if @tree.errors.any?
    #error_explanation
      %h2
        = pluralize(@tree.errors.count, "error")
        prohibited
        this tree from being saved:
      %ul
        - @tree.errors.full_messages.each do |msg|
          %li= msg
  %p
    = f.label :name
    %br/
    = f.text_field :name
  %p
    = f.submit
